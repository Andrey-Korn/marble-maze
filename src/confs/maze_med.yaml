# configuration for the harder BRIO maze board (insert)

fast_thresh: 30

# corner offsets
# (tl_offset, tr_offset, bl_offset, br_offset)
# only setup for 1080p currently
offsets:
  - 100
  - 100
  - 100
  - 100
